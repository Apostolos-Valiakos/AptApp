# Without this, the Dockerfile's `COPY . .` pulls all of this into the build
# context and bakes it into intermediate image layers — including real
# secrets in .env and the full git history.
.env
.git
.gitignore
node_modules
dist
client/dist
android
capacitor.config.json
*.sql
*.dump
scripts/__pycache__/
server/tests/
Installation Steps.pdf
.vscode
.claude
DEPLOY.md
README.md
