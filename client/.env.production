# Base URL the native Android/iOS app uses for API and Socket.IO calls.
# Relative /api and /socket.io requests don't work in the native app (no Vite dev proxy,
# and capacitor://localhost has no backend of its own) — see client/src/main.ts.
VITE_API_BASE_URL=https://interventio.gr
